services:
  - type: web
    name: telegram-dual-ai-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
```

---

## 📁 Keep `runtime.txt` same
```
python-3.11.0
